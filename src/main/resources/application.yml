# �����˿�
server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/self?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    password: admin123
    username: root
  main:
    allow-circular-references: true
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.zc.followheart.entity
  # mybatis��־��ӡ
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


